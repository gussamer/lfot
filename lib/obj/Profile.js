#!/usr/bin/env node
'use strict'
/**
 * Profile handles the "special" nature of salesforce profile xml
 */
 module.exports = class Profile{
    constructor(args){
      //if no list of profile names supplied then build list of all
      //build list of metadata types to retrieve with list of profiles
    }
    getXML(){
      return this.xml
    }
}
